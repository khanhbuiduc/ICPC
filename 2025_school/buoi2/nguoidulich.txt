cạnh min: (u-v) = cmin
back_track( i ) // lần thứ i thăm thành phố.
	duyệt city: 1->n 
		nếu city chưa thăm
			visited[ city ] = true
			x[i] = city;
			cost += c[ x[i-1] ][ X[i] ]
			nêu  i == n
				ans = min(ans, cost + c[X[n]] [1] )
			else nếu ans > (d + (n-1+1)*cmin)
				recusion( i+1 )
			// back track
			visited[j]  = 0
			cost -= c[X[n]] [1];
			