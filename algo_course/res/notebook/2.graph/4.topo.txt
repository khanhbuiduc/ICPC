vector<int> store; // store result in reverse order

void dfsTopo(int u)
{
    visited[u] = true;
    for (int v : adj[u])
    {
        if (!visited[v])
            dfsTopo(v);
    }
    store.push_back(u);
}


