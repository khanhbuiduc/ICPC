vector<int> store;//store bị ngược
void dfsTopo(int u)
{
    visited[u] = true;
    for (int v : adj[u])
    {
        if (visited[v] == false)
            dfsTopo(v);
    }
    store.push(u);
}